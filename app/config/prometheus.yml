global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Python bridge — exports temporal_* and phoenix_* metrics from cloud APIs
  - job_name: poc_app_metrics
    static_configs:
      - targets:
          - unified_dash-app:8001
    metrics_path: /metrics
